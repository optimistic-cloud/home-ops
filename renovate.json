{
  "regexManagers": [
    {
      "fileMatch": ["^Justfile$"],
      "matchStrings": [
        "RESTIC_DOCKER_IMAGE := \"(?<depName>restic/restic):(?<currentValue>.*?)\""
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
