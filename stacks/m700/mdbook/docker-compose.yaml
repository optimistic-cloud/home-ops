name: mdbook

services:
  mdbook:
    container_name: mdbook
    image: peaceiris/mdbook:v0.5.0@sha256:a82fd90af897238521c17e4b64479f5d56b578e4776198788256086b3a59617a
    restart: unless-stopped
    user: "1000:1000"
    stdin_open: true
    tty: true
    expose:
      - 3000
      - 3001
    volumes:
      - ./mdbook-data:/book
    environment:
      TZ: Europe/Berlin
    command:
      - serve
      - --hostname
      - '0.0.0.0'
    networks:
      - m700_default

networks:
  m700_default:
    external: true
